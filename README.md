# Flight_reservation_system
A web app that manages flights, users, and bookings using core data structures. It handles passenger profiles via a Binary Search Tree (BST) for fast $O(\log n)$ log-ins, uses Singly Linked Lists for dynamic flight updates, and simulates database persistence using browser storage, bringing low-level logic into a modern interface.
